#!/command/with-contenv bash
# ==============================================================================
# Halt the add-on if the API service exits, so HA Supervisor can restart it.
# ==============================================================================

echo "[api] service exited — bringing the add-on down."

exec /run/s6/basedir/bin/halt
